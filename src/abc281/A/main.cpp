#include <bits/stdc++.h>

using namespace std;

void solve(long long N) {
    for (int i = N; i >= 0; i--) cout << i << endl;
}

int main() {
    long long N;
    std::scanf("%lld", &N);

    solve(N);
    return 0;
}
